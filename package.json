{
  "name": "ui-startkit-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run coverage -w=ui",
    "dev": "npm run storybook -w=ui",
    "build": "npm run build --workspaces",
    "serve": "npx http-server ./packages/ui/storybook-static",
    "publish-cli": "npm publish -w=cli"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.2.1",
    "prettier": "^3.6.2"
  }
}
